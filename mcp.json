{
    "mcpServers": {
        "trae-phone-mcp": {
            "command": "python",
            "args": [
                "-m",
                "trae_phone_mcp"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    }
}