{
    "mcpServers": {
        "phone-mcp": {
            "command": "python",
            "args": [
                "-m",
                "phone_mcp"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    }
}